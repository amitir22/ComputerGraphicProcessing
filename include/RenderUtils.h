#include <Eigen/Dense>

float EdgeFunction(const Eigen::Vector3f& a, const Eigen::Vector3f& b, const Eigen::Vector3f& c);

int GetIntMin(float a, float b, float c);
int GetIntMax(float a, float b, float c);